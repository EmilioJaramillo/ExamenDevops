package cl.duoc.logistica.inventarios;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class InventariosApplicationTests {

    @Test
    void contextLoads() {
        // Verifica que el contexto de Spring (incluyendo la conexión a la base de datos)
        // levante correctamente. Sirve como smoke test en el pipeline de CI/CD.
    }
}
